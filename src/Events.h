#pragma once

namespace Events {
using ContainerChanged = RE::TESContainerChangedEvent;
using Equip = RE::TESEquipEvent;
using SpellCast = RE::TESSpellCastEvent;
using SwitchRaceComplete = RE::TESSwitchRaceCompleteEvent;
}
